# Copy this file to `.env` and fill in. Never commit `.env`.

# --- OpenAI (optional if you use Ollama) ---
# https://platform.openai.com/api-keys — requires usable quota / billing if you see 429 insufficient_quota
OPENAI_API_KEY=

# --- Local Ollama (free) — install from https://ollama.com then: ollama pull llama3.2 ---
# USE_OLLAMA=1
# OLLAMA_MODEL=llama3.2
